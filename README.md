炎

Prerequisites:

- [Vercel CLI](https://vercel.com/docs/cli) installed globally

To develop locally:

```
pnpm install
vc dev
```

```
open http://localhost:3000
```

To build locally:

```
pnpm install
vc build
```

To deploy:

```
pnpm install
vc deploy
```
